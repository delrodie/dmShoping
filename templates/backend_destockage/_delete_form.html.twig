<form method="post" action="{{ path('backend_destockage_delete', {'id': destockage.id}) }}" onsubmit="return confirm('Voulez-vous vriment supprimer ce destockage?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ destockage.id) }}">
    <button class="btn btn-danger btn-raised mb-0">Supprimer</button>
</form>
