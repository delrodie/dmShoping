{% extends 'base.html.twig' %}

{% block title %}Artiste{% endblock %}

{% block body %}
    <h1>Artiste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiste.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artiste.nom }}</td>
            </tr>
            <tr>
                <th>Media</th>
                <td>{{ artiste.media }}</td>
            </tr>
            <tr>
                <th>NombreAlbum</th>
                <td>{{ artiste.nombreAlbum }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ artiste.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_artiste_index') }}">back to list</a>

    <a href="{{ path('backend_artiste_edit', {'id': artiste.id}) }}">edit</a>

    {{ include('backend_artiste/_delete_form.html.twig') }}
{% endblock %}
