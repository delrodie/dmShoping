<form method="post" action="{{ path('backend_album_delete', {'id': album.id}) }}" onsubmit="return confirm('Voulez-vous supprimer cet album?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    <button class="btn btn-danger btn-raised mb-0">Supprimer</button>
</form>
