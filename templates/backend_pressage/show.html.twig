{% extends 'base.html.twig' %}

{% block title %}Pressage{% endblock %}

{% block body %}
    <h1>Pressage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pressage.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ pressage.quantite }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pressage.date }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_pressage_index') }}">back to list</a>

    <a href="{{ path('backend_pressage_edit', {'id': pressage.id}) }}">edit</a>

    {{ include('backend_pressage/_delete_form.html.twig') }}
{% endblock %}
