{{ form_start(form) }}
<div class="row form-group">
    <div class="col-sm-12 mt-3">{{ form_row(form.album) }}</div>
    <div class="col-sm-12 mt-3">{{ form_row(form.quantite) }}</div>
    <div class="col-sm-12 mt-3">{{ form_row(form.pu) }}</div>
</div>
{{ form_widget(form) }}
    <div class="text-right mt-5">
        <input type="hidden" name="ancienne_quantite" value="{{ vente.quantite }}">
        <input type="hidden" name="ancien_montant" value="{{ vente.montant }}">
        <a href="{{ path('backend_facture_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('Ajouter') }}</button>
    </div>
{{ form_end(form) }}
