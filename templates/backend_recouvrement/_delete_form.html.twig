<form method="post" action="{{ path('backend_recouvrement_delete', {'id': recouvrement.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce recouvrement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recouvrement.id) }}">
    <button class="btn btn-danger btn-raised mb-0">Supprimer</button>
</form>
