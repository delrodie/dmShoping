{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ genre.libelle }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ genre.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_genre_index') }}">back to list</a>

    <a href="{{ path('backend_genre_edit', {'id': genre.id}) }}">edit</a>

    {{ include('backend_genre/_delete_form.html.twig') }}
{% endblock %}
