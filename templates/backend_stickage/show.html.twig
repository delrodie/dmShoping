{% extends 'base.html.twig' %}

{% block title %}Stickage{% endblock %}

{% block body %}
    <h1>Stickage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stickage.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ stickage.date }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ stickage.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_stickage_index') }}">back to list</a>

    <a href="{{ path('backend_stickage_edit', {'id': stickage.id}) }}">edit</a>

    {{ include('backend_stickage/_delete_form.html.twig') }}
{% endblock %}
