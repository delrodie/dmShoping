<form method="post" action="{{ path('backend_localite_delete', {'id': localite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ localite.id) }}">
    <button class="btn">Delete</button>
</form>
