{{ form_start(form) }}
<div class="row form-group">
    <div class="col-sm-12 mt-3">{{ form_row(form.vendeur) }}</div>
    <div class="col-sm-12 mt-3">{{ form_row(form.date) }}</div>
    <div class="col-sm-12 form-check mb-2 mt-3 ml-3">
        {{ form_widget(form.tva) }}
        {{ form_label(form.tva) }}
    </div>
</div>
{{ form_widget(form) }}
    <div class="text-right mt-5">
        <a href="{{ path('backend_facture_index') }}" class="btn btn-outline-secondary">Annuler</a>
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
