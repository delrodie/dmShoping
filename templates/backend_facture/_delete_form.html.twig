<form method="post" action="{{ path('backend_facture_delete', {'id': facture.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette facture?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.id) }}">
    <button class="btn btn-danger btn-raised mb-0">Supprimer</button>
</form>
